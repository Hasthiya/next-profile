"use client"

import {Button} from "@/components/ui/button";

const Nav = () => {
    return (
        <header>
            <nav>
                <Button>{"Shadcn button"}</Button>
            </nav>
        </header>
    );
};

export default Nav;